<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chat Session History</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen">
    <div class="container mx-auto px-4 py-8">
        <h1 class="text-3xl font-semibold text-center mb-8 text-gray-800">Chat Session History</h1>
        <div id="chatbot" class="w-full md:w-3/5 lg:w-2/5 mx-auto relative border border-black rounded-xl h-[80vh] flex flex-col overflow-hidden shadow-lg bg-white">
            <div id="chat-messages" class="flex-1 overflow-y-auto p-4 space-y-2">
                <% chats.forEach(chat => { %>
                    <div class="p-2 rounded-lg <%= chat.sender === 'You' ? 'bg-neutral-600 text-zinc-300 ml-auto' : 'bg-gray-200 text-gray-800' %>" style="max-width: 80%;">
                        <p class="font-semibold"><%= chat.sender %></p>
                        <p><%= chat.message %></p>
                        <p class="text-xs text-gray-500 mt-1"><%= new Date(chat.timestamp).toLocaleString() %></p>
                    </div>
                <% }); %>
            </div>
        </div>
        <div class="mt-8 text-center">
            <a href="/history" class="bg-neutral-600 text-zinc-300 px-6 py-3 rounded-md hover:bg-zinc-800 transition duration-150 ease-in-out mr-4">Back to History</a>
            <a href="/chatpage" class="bg-neutral-600 text-zinc-300 px-6 py-3 rounded-md hover:bg-zinc-800 transition duration-150 ease-in-out">Start New Chat</a>
        </div>
    </div>
</body>
</html>